import { PlaceholderPage } from "@/components/placeholder-page";

export default function Page() {
  return (
    <PlaceholderPage
      title="Environments"
      description="Base URLs, allowlists, and safety policy."
    />
  );
}
